<div>
    <div>
        <h3><%= `${contact.firstName} ${contact.lastName}` %></h3>
        <p><%= contact.email %></p>
        <p><%= contact.phone %></p>
        <p><%= contact.address %></p>
    </div>
    <div>
      <a href="/agenda/<%= contact.agendaId %>/contato/editar/<%= contact.id %>">Editar</a>
      <a href="/agenda/<%= contact.agendaId %>/contato/deletar/<%= contact.id %>">Excluir</a>
    </div>
  </div>